version: '2'
services:
  mongodb:
    image: tutum/mongodb
    environment:
      - MONGO_INITDB_DATABASE="status"
      - MONGO_INITDB_ROOT_USERNAME="sa"
      - MONGO_INITDB_ROOT_PASSWORD="sa"
    ports:
      - "27017:27017"
    network_mode: bridge